export const dynamic = "force-dynamic";

export const metadata = {
  title: "Menu — Samsara Group",
  description: "Explore the Samsara Group menu across dining, cocktails, and curated experiences.",
};

export default function MenuLayout({ children }) {
  return children;
}
